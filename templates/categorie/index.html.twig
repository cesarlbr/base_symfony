{% extends 'base.html.twig' %}

{% block title %}Ajout categorie{% endblock %}

{% block body %}

    {{ form_start(form, {'attr': {"class":"w-full max-w-md mx-auto p-6 mt-24"} } ) }}
        {{ form_label(form.nom) }}
        {{ form_widget(form.nom) }}
        <input type="submit" value="Ajouter" class="w-full py-3 px-4 inline-flex justify-center items-center gap-x-2 text-sm font-semibold rounded-lg border border-transparent bg-blue-600 text-white hover:bg-blue-700 disabled:opacity-50 disabled:pointer-events-none dark:focus:outline-none dark:focus:ring-1 dark:focus:ring-gray-600">
    {{ form_end(form) }}
    {% include "_componants/_toast.html.twig" %}
{% endblock %}
